#!/bin/bash
echo "Bash version ${BASH_VERSION}..."
for n_epochs in $(seq 200 100 1000)
    do
        for batch_size in $(seq 1 1 13)
            for batch_size in $(seq 1 1 13)
                do
                    do
                        echo "n_epochs: $n_epochs, batch_size: $((2**$batch_size))"
                    done
                done
    done